<!DOCTYPE html>
<html>
  <head>
    <title>Weeknotes Collector</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta name="format-detection" content="telephone=no" />
    <meta name="routePrefix" content="/{{ routePrefix }}" />
    <link rel="stylesheet" href="{{ routePrefix }}/css/style.css" />
    <script src="{{ routePrefix }}/js/main.js" defer></script>
  </head>
  <body>
    <main class="min-h-screen">
      <div class="max-w-4xl mx-auto px-8 py-12">
        <header class="mb-8">
          <h1 class="text-4xl font-bold text-white mb-2">
            Weeknotes Collector
          </h1>
          <p class="text-gray-500">Save links across the web for curation</p>
          {% if livePage != "" %}
          <p class="text-gray-500">Live: <a class="text-white hover:underline" href="{{ livePage }}">{{ livePage }}</a></p>
          {% endif %}
        </header>

        {% comment %}<!-- prettier-ignore -->{% endcomment %}
        {% include "_link-section.liquid", title: "Create", links: links.create %}

        {% comment %}<!-- prettier-ignore -->{% endcomment %}
        {% include "_link-section.liquid", title: "Consume", links: links.consume %}

        <section class="flex gap-3">
          <button
            class="export-btn px-8 py-3 bg-blue-600 text-white text-sm font-bold rounded-lg hover:bg-blue-500 transition-colors"
          >
            Export
          </button>
          <button
            class="clear-btn px-8 py34 bg-red-600 text-white text-sm font-bold rounded-lg hover:bg-red-500 transition-colors"
          >
            Clear
          </button>
        </section>
      </div>
    </main>
  </body>
</html>
